{{!-- template called by inside the category_items.handlebars file to show all the items from the database
May need an else statement for error placeholder cards --}}
<div class="col-10 col-md-8 col-lg-6 col-xl-4 mx-auto">
    <div class="card p-1 m-2 text-center shadow-lg rounded-lg">
        <div class="card-header text-secondary px-0 px-0">
          <h4><span class="badge badge-pill badge-primary"> Item: </span> {{dataValues.name}}</h4>
        </div>
        <div class="card-body p-1 font-weight-bold">
            <img src="../images/{{dataValues.image_name}}" class="item-image" alt="Item Display image">
            <p class="card-text">{{dataValues.description}}</p>
            <p class="card-text">ItemID: <span class="badge badge-pill badge-primary py-2 mt-1 mb-0">{{dataValues.id}}</span> |  
            Cost: <span class="badge badge-pill badge-primary py-2 mt-1 mb-0"> ${{dataValues.price}}</span></p>
        </div>
        <div class="card-footer">
            {{#if user}}
            {{!-- If a user display add to cart button --}}
                <button type="submit" class="btn badge-pill btn-sm btn-primary add-item" data-id="{{dataValues.id}}" data-price="{{dataValues.price}}" value="{{dataValues.id}}"
                data-toggle="tooltip" title="Add {{dataValues.name}} to your cart.">
                    Add To Cart <i class="fas fa-cart-plus"></i>
                </button>
            {{else}}
            {{!-- If not logged in user sees a log in to add to cart --}}
            <div class="btn-group">
                <a title="Log-in / Sign up to add an Item to cart" class="btn badge-pill btn-sm btn-primary" href="/login"><i class="fas fa-sign-in-alt"> Log-In</i></a>
                <button disabled aria-disabled="true" class="btn badge-pill btn-sm btn-primary"
                data-toggle="tooltip" title="Log-in to add {{dataValues.name}} to your cart.">
                   to add to Cart <i class="fas fa-cart-plus"></i>
                </button>
            </div>
            {{/if}}
        </div>
    </div>
</div>